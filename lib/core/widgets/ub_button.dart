import 'package:flutter/material.dart';

class UBButton extends StatelessWidget {
  const UBButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}